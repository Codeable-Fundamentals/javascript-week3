// Forma clasica de escribir funciones
// declarativa de funcion 
function sumar(a, b) {
  return a + b;
}

// funciones como expresion - expresion de funcion
const restar = function (a, b) {
  return a - b;
};

// arrow functions - funciones flecha
const multiplicar = (a, b) => a * b;
