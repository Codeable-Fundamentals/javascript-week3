// funcion Declarativa 
function sumar(a, b) {
  // el codigo va aqui
}

// funcion anonima (Funcion expresion)
const sumar_2 = function (a, b) {
  // el codgio va aqui
};

// arrow function
// =>
const sumar_3 = (a, b) => a + b;

// let nombreFuncion = (arg1, arg2, ..., argN) => expresión;

console.log(sumar_3(2,3)) // 5